apiVersion: v1
kind: Service
metadata:
  name: {{ include "argocd-ecr-updater.fullname" . }}
  labels:
    {{- include "argocd-ecr-updater.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argocd-ecr-updater.selectorLabels" . | nindent 4 }}
